---
phase: 07-visual-design-system
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Calculator looks identical to before — no visual regressions"
    - "Design tokens are reusable for future components in phases 8-11"
    - "All UI elements use consistent color palette"
    - "Typography hierarchy is clear and readable"
  artifacts: []
  key_links: []
---

<objective>
Verify the design system refactor produced no visual regressions and the token system is working correctly.

Purpose: Confirm the foundation is solid before phases 8-11 build on it.
Output: Verified visual design system ready for downstream phases.
</objective>

<execution_context>
@/Users/angel-ai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angel-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visual-design-system/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of design system</name>
  <files>src/styles/design-tokens.css, src/styles/Calculator.css, src/index.css</files>
  <action>
Design token system (CSS custom properties) applied to all calculator styles. Every hardcoded color, font size, spacing, border radius, shadow, and transition has been replaced with token references. The calculator should look exactly the same as before.

Human verification required — run `npm run dev` and visually confirm no regressions.
  </action>
  <verify>
1. Run `npm run dev` to start the dev server
2. Open the calculator in browser
3. Verify these visual checks:
   - Calculator body: Dark background (#2c2c2c), rounded corners, shadow
   - Display: Green-tinted background (#4a5a3a), dark text, expression + result visible
   - Number buttons: Gray (#4a4a4a), white text, hover brightens
   - Operator buttons: Orange (#ff9500), white text
   - Clear button: Light gray (#a5a5a5), dark text
   - Scientific toggle: "SCI" button, blue when active
   - Scientific panel: Blue-gray buttons, slides in from left
   - Memory buttons: Green-tinted, subdued text
   - Graph controls: Input field with blue focus border, Plot/Clear buttons
   - History panel: Entries with hover states
4. Test responsive behavior:
   - Resize to phone width (under 480px) — buttons and text scale down, panels stack
   - Resize to tablet (481-768px) — intermediate sizing
5. Verify no broken styles or visual glitches
  </verify>
  <done>User approves that calculator looks identical to pre-refactor state with no visual regressions.</done>
</task>

</tasks>

<verification>
Human confirms visual appearance matches pre-refactor state.
</verification>

<success_criteria>
- User approves that calculator looks identical to before the refactor
- No visual regressions reported
- Design system is confirmed working and ready for phases 8-11
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-design-system/07-02-SUMMARY.md`
</output>
